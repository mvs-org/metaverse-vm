[package]
authors     = ["Hyperspace Network <contact@mvs.org>"]
description = "MODEXP precompiles for EVM pallet."
edition     = "2018"
homepage    = "https://mvs.org/"
license     = "GPL-3.0"
name        = "hyperspace-evm-precompile-modexp"
readme      = "README.md"
repository  = "https://github.com/mvs-org/Hyperspace"
version     = "2.0.0"


[dependencies]
# crates
dp-evm = { default-features = false, path = "../../../../../primitives/evm" }
evm    = { default-features = false, version = "0.23.0", features = ["with-codec"] }
num    = { default-features = false, version = "0.3", features = ["alloc"] }
# substrate
sp-core = { default-features = false, git = "https://github.com/new-mvs/substrate.git", branch = "latest" }
sp-io   = { default-features = false, git = "https://github.com/new-mvs/substrate.git", branch = "latest" }

[dev-dependencies]
hex = "0.4.0"

[features]
default = ["std"]

std = [
	"crates-std",
	"substrate-std",
]

crates-std = [
	"dp-evm/std",
	"evm/std",
	"num/std",
]

substrate-std = [
	"sp-core/std",
	"sp-io/std",
]
